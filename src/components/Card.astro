---
import { Image } from 'astro:assets';

interface Props {
	title: string;
	href: string;
	postedOn: Date;
}

const { href, title, postedOn } = Astro.props;

---

<div class="card w-96 bg-base-100 shadow-xl">
	<div class="card-body">
	  <h2 class="card-title">{title}</h2>
	  <p>If a dog chews shoes whose shoes does he choose?</p>
	</div>
  </div>